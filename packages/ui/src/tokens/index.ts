/**
 * Re-export design tokens for ergonomic access from UI components.
 * UI components should import from `../tokens` instead of `@arac-hasar/design`
 * directly so the dependency is internalized at the package boundary.
 */
export * from '@arac-hasar/design';
