# Backend API URL — FastAPI development server
NEXT_PUBLIC_API_URL=http://localhost:8000

# Optional: API key for backend authentication (forwarded by /api/inspect proxy)
# BACKEND_API_KEY=dev-secret
